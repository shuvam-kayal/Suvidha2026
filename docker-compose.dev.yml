# Development overrides for docker-compose.yml
# Usage: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up

version: '3.8'

services:
  api-gateway:
    build:
      context: ./apps/api-gateway
      dockerfile: Dockerfile.dev
    environment:
      ENVIRONMENT: development
    volumes:
      - ./apps/api-gateway/app:/app/app:ro
    ports:
      - "3000:3000"

  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile.dev
    environment:
      ENVIRONMENT: development
    volumes:
      - ./services/auth-service/app:/app/app:ro
    ports:
      - "3001:3001"

  billing-service:
    build:
      context: ./services/billing-service
      dockerfile: Dockerfile.dev
    environment:
      ENVIRONMENT: development
    volumes:
      - ./services/billing-service/app:/app/app:ro
    ports:
      - "3002:3002"

  grievance-service:
    build:
      context: ./services/grievance-service
      dockerfile: Dockerfile.dev
    environment:
      ENVIRONMENT: development
    volumes:
      - ./services/grievance-service/app:/app/app:ro
    ports:
      - "3003:3003"

  kiosk-ui:
    build:
      context: ./apps/kiosk-ui
      dockerfile: Dockerfile.dev
    volumes:
      - ./apps/kiosk-ui/src:/app/src:ro
      - ./apps/kiosk-ui/public:/app/public:ro
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:3000

  admin-portal:
    build:
      context: ./apps/admin-portal
      dockerfile: Dockerfile.dev
    volumes:
      - ./apps/admin-portal/src:/app/src:ro
      - ./apps/admin-portal/public:/app/public:ro
    ports:
      - "5174:5174"
    environment:
      - VITE_API_URL=http://localhost:3000
